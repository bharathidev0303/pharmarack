import 'package:flutter/material.dart';

class ChangePasswordScreenWebView extends StatelessWidget {
  const ChangePasswordScreenWebView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Web View");
  }
}
