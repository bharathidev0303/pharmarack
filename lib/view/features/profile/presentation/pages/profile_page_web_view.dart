import 'package:flutter/material.dart';

/// This class [ProfilePageWebView] which specifically used to render Web UI
class ProfilePageWebView extends StatelessWidget {
  const ProfilePageWebView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("PROFILE"),
    );
  }
}
